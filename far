-- إنشاء واجهة المستخدم (ScreenGui)
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "BanNotification"
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- إنشاء النافذة الرئيسية (Frame)
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 300, 0, 150)
frame.Position = UDim2.new(0.5, -150, 0.5, -75)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30) -- لون مات بلاك
frame.BorderSizePixel = 0
frame.Parent = screenGui

-- إضافة نص "متبند"
local textLabel = Instance.new("TextLabel")
textLabel.Size = UDim2.new(1, 0, 0.6, 0)
textLabel.Text = "أنت متبند من السكربت"
textLabel.TextColor3 = Color3.fromRGB(255, 0, 0) -- لون أحمر للتنبيه
textLabel.BackgroundTransparency = 1
textLabel.Font = Enum.Font.GothamBold
textLabel.TextSize = 20
textLabel.Parent = frame

-- إضافة زر لـ "طرد" اللاعب (Kick)
local kickButton = Instance.new("TextButton")
kickButton.Size = UDim2.new(0, 120, 0, 40)
kickButton.Position = UDim2.new(0.5, -60, 0.7, 0)
kickButton.Text = "إغلاق"
kickButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
kickButton.TextColor3 = Color3.new(1, 1, 1)
kickButton.Parent = frame

-- وظيفة عند الضغط على الزر لطرده من السيرفر
kickButton.MouseButton1Click:Connect(function()
    game.Players.LocalPlayer:Kick("لقد تم حظرك من السكربت نهائياً.")
end)
